$(document).ready(function () {
  $('.slider').slick({
    arrows: true,
    dots: false,
    // adaptiveHeight: true,
    // slidesToShow: 2,
    // slidesToScroll: 2,
    speed: 1000,
    easeing: 'linear',
    infinite: true,
    autoplay: true
  });
});
